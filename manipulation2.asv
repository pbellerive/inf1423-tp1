% Étudiant  : Patrick Bellerive
% Matricule: ET470765
% Matlab : R2018b
% OS: Windows 10

img = imread("lena.gif");

N = 4;
n1 = 2;
result = lz77V2(img, N, n1);
disp(strlength(result));

N = 8;
n1 = 4;
result = lz77V2(img, N, n1);
disp(strlength(result));

N = 16;
n1 = 8;
result = lz77V2(img, N, n1);
disp(strlength(result));